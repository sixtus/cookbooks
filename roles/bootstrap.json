{
  "name": "bootstrap",
  "description": "bootstrap role",
  "run_list": [
    "recipe[ohai]",
    "recipe[base]",
    "recipe[openssl]",
    "recipe[ssh::server]",
    "recipe[bash]",
    "recipe[git]",
    "recipe[vim]"
  ]
}
