# this file can be used to deploy a custom gitconfig to ~/.gitconfig.local
# which will be sourced by the globally deployed gitconfig.
# use templates/default/user-jdoe/gitconfig.local to override this file.
